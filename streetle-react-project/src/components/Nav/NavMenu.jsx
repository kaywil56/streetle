import Help from "../Modals/Help";
import Settings from "../Modals/Settings";

const NavMenu = () => {
  return (
    <nav>
      Nav
      <Help />
      <Settings />
    </nav>
  );
};

export default NavMenu;
